# Genesis graph (v0)

Safety default: **no pack runs unless its hash is pinned.**
Compute stays local (OpenClaw + spawn.py). Arweave is optional freeze-frame.

See `code/spawn/pins.json` and `code/spawn/mount_genesis.py`.
Packs are JSON/Markdown only. `exec: false`. No extra pointer hops.
